import { DateAdapterHelper } from './date-adapter-helper';

describe('DateAdapterHelper', () => {
  it('should create an instance', () => {
    expect(new DateAdapterHelper()).toBeTruthy();
  });
});
