import { UpdateUserPassword } from './update-user-password';

describe('UpdateUserPassword', () => {
  it('should create an instance', () => {
    expect(new UpdateUserPassword()).toBeTruthy();
  });
});
