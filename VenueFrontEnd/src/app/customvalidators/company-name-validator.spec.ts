import { CompanyNameValidator } from './company-name-validator';

describe('CompanyNameValidator', () => {
  it('should create an instance', () => {
    expect(new CompanyNameValidator()).toBeTruthy();
  });
});
